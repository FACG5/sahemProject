<!DOCTYPE html>
<html lang="en">

        {{>head}}
        <body>
        <div class="container">

        {{>header}}
        {{{body}}}
        
        </div>
    <script src="{{js}}"></script>
</body>
</html>